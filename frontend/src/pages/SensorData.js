import React from 'react';
import SensorTable from '../components/SensorTable';

function SensorData() {
  return (
    <div>
      <h1 className="page-heading gold-text">Sensor Dashboard</h1>
      <SensorTable />
    </div>
  );
}

export default SensorData;
